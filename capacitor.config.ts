import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Group4_IT005_FinalProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
